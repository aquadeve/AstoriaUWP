<Page
    x:Class="DalvikUWPCSharp.ApkConverterPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DalvikUWPCSharp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="56"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Background="{ThemeResource SystemControlBackgroundChromeMediumBrush}">
            <TextBlock Text="APK Converter"
                       VerticalAlignment="Center"
                       Margin="16,0,0,0"
                       FontSize="20"
                       FontWeight="SemiBold"/>
        </Grid>

        <!-- File Selection Area -->
        <StackPanel Grid.Row="1" Margin="12,8,12,4" Orientation="Vertical">
            <TextBlock Text="Select an APK file to analyse and convert for use with AstoriaUWP."
                       TextWrapping="Wrap"
                       Margin="0,0,0,8"
                       FontSize="13"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="apkPathBox"
                         Grid.Column="0"
                         PlaceholderText="No APK selected"
                         IsReadOnly="True"
                         Margin="0,0,8,0"
                         VerticalAlignment="Center"/>
                <Button x:Name="browseButton"
                        Grid.Column="1"
                        Content="Browse..."
                        Click="browseButton_Click"
                        VerticalAlignment="Center"/>
            </Grid>

            <!-- Action Buttons -->
            <StackPanel Orientation="Horizontal" Margin="0,8,0,4">
                <Button x:Name="analyzeButton"
                        Content="Analyze APK"
                        IsEnabled="False"
                        Click="analyzeButton_Click"
                        Margin="0,0,8,0"/>
                <Button x:Name="convertButton"
                        Content="Convert &amp; Install"
                        IsEnabled="False"
                        Click="convertButton_Click"
                        Margin="0,0,8,0"/>
                <Button x:Name="clearButton"
                        Content="Clear"
                        Click="clearButton_Click"/>
            </StackPanel>

            <!-- Progress Bar -->
            <ProgressBar x:Name="progressBar"
                         IsIndeterminate="True"
                         Visibility="Collapsed"
                         Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Log / Report Area -->
        <ScrollViewer Grid.Row="2" Margin="12,0,12,8" VerticalScrollBarVisibility="Auto">
            <TextBox x:Name="logBox"
                     IsReadOnly="True"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     FontFamily="Consolas"
                     FontSize="11"
                     Text="APK Converter ready. Select an APK file to begin."
                     VerticalAlignment="Stretch"
                     MinHeight="200"/>
        </ScrollViewer>

        <!-- Bottom bar -->
        <Grid Grid.Row="3" Margin="12,0,12,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="statusText"
                       Grid.Column="0"
                       VerticalAlignment="Center"
                       FontSize="12"
                       Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                       Text="Ready"/>
            <Button x:Name="backButton"
                    Grid.Column="1"
                    Content="Back to Apps"
                    Click="backButton_Click"/>
        </Grid>
    </Grid>
</Page>
